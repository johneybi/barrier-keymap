section: screens
	server:
	mac:
	windows:
end

section: links
	server:
		right = mac
		left = windows
	mac:
		left = server
	windows:
		right = server
end

section: remaps
	mac:
		right_alt.alone = F19
		right_alt.hold = right_super
		control+space = F19

	windows:
		left_super = left_control
		right_super.alone = hangul
end

section: options
end
