section: screens
	server:
	mac:
	windows:
end

section: links
	server:
		right = mac
		left = windows
	mac:
		left = server
	windows:
		right = server
end

section: remaps
	mac:
		right_alt.alone = F16
		right_alt.hold = right_super
		hangul.alone = F16
		hangul.hold = right_super
		control+space = F16

	windows:
		left_super = left_control
end

section: options
end
